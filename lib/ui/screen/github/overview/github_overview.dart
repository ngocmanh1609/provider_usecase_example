import 'package:flutter/material.dart';

class GithubOverview extends StatelessWidget {
  const GithubOverview({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Github Repositories"),
      ),
      body: Center(
        child: Text("TODO"),
      ),
    );
  }
}
